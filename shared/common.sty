\ProvidesPackage{common}

\usepackage{kvoptions}
\SetupKeyvalOptions{
family=thesis,
prefix=thesis@
}
\DeclareBoolOption[false]{final}
\ProcessKeyvalOptions*

% package phase #1
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[portuguese,english]{babel}

% package phase #2
\usepackage[table]{xcolor}

% package phase #3
\usepackage[algoruled,lined,longend,linesnumbered]{algorithm2e}
\usepackage{amsmath}
\usepackage[printonlyused]{acronym}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{indentfirst}
\usepackage{mathtools}
\usepackage{minted}
\usepackage{paralist}%  inparaenum environment: inline lists
\usepackage{pdfpages}    % enables \includepdf
\usepackage{relsize}
\usepackage{subfiles}
\usepackage{xspace}

\ifthesis@final
	\RequirePackage[disable]{todonotes}
\else
	\RequirePackage{todonotes}
\fi

\usepackage{varioref}
\ifthesis@final
	\usepackage[colorlinks=false]{hyperref}
\else
	\usepackage[colorlinks=true]{hyperref}
\fi
\usepackage[noabbrev,capitalise]{cleveref}
\usepackage{breakurl}

\usepackage{csquotes}
\usepackage[backend=biber,bibencoding=utf8,maxbibnames=10,maxcitenames=2,sortcites=true,sorting=none,urldate=comp]{biblatex}
\usepackage[retainorgcmds]{IEEEtrantools}

%%%%% %%%%% %%%%% %%%%% %%%%%
%%%%%       HACKS       %%%%%
%%%%% %%%%% %%%%% %%%%% %%%%%

% ceil delimiters
\DeclarePairedDelimiter{\ceil}{\lceil}{\rceil}
\DeclarePairedDelimiter\norm{\lVert}{\rVert}

% minted C++ code snippets

% equation label for the ieeeqnarray environment
\newcommand{\IEEElabel}[1]{\begingroup\addtocounter{equation}{-1}%
\refstepcounter{equation}\label{#1}\endgroup}

%	Paragraph (title) with linebreak (like subsubsubsection)
\newcommand{\paragraphh}[1]{\paragraph{#1\hfill}\hfill

\smallskip
}

% cite with author name
\newcommand{\citewithauthor}[1]{\citeauthor{#1}\xspace\cite{#1}}
